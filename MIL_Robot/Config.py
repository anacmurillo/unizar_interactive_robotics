
#Defines if you want to combine Mask RCNN and the WaterShed algorithm or just WaterShed (If you use both, you need to have Detectron install)
USE_MASKRCNN = True

#Defines if you want to give the commands using the voice or using the keyboard (The speech recognition uses the google api and adds delay to the system)
USE_VOICE_RECOG = True

#Defines if the system should write the text or speak throught the speakers (The text to speech uses the google api and adds delay to the system)
USE_SPEECH = True